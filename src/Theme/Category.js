import I18n, {strings} from '../Theme/Resource/language/i18n';
import {localize} from '../Theme/Constants'
const Category = [localize('all'), localize('category'), localize('top'), localize('recomendation')];
export default Category